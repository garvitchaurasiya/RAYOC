const routes = require('next-routes')(); // Notice that how we have put second set of panenthesis here. It's because the require statement right here returns a function and by putting panenthesis we invoked it imediately.



module.exports = routes;